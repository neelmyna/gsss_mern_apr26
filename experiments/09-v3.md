# Question 9
```
Develop a React code to build a simple search filter functionality to display a filtered list based on the
search query entered by the user.
```

# MERN Student Search Filter Application

This version uses:

* React Frontend
* Express Backend
* MongoDB Database
* Student Collection
* Dynamic Search Filter

---

# Folder Structure

```text id="jlwmca"
project/
│
├── backend/
│   ├── server.js
│   ├── package.json
│
└── frontend/
    ├── src/
    │   ├── App.js
    │   └── index.js
    └── package.json
```

---

# BACKEND SETUP

---

# Step 1: Create Backend

```bash id="jlwmcb"
mkdir backend
cd backend
npm init -y
```

---

# Step 2: Install Packages

```bash id="jlwmcc"
npm install express cors mongoose
```

---

# Step 3: MongoDB Database

Database:

```text id="jlwmcd"
college
```

Collection:

```text id="jlwmce"
students
```

---

# Sample MongoDB Documents

```javascript id="jlwmcf"
use college
```

```javascript id="jlwmcg"
db.students.insertMany([

{
    name: "Somu",
    dept: "CSE",
    sem: 5
},

{
    name: "Arun",
    dept: "ECE",
    sem: 4
},

{
    name: "Priya",
    dept: "ISE",
    sem: 6
},

{
    name: "Karthik",
    dept: "CSE",
    sem: 3
},

{
    name: "Meena",
    dept: "EEE",
    sem: 7
}

])
```

---

# Step 4: Create Express + MongoDB Server

# backend/server.js

```javascript id="jlwmch"
const express = require('express');

const mongoose = require('mongoose');

const cors = require('cors');

const app = express();

const PORT = 3000;


// Middleware

app.use(cors());

app.use(express.json());



// MongoDB Connection

mongoose.connect(
    'mongodb://127.0.0.1:27017/college'
)

.then(() => {

    console.log("MongoDB Connected");
})

.catch((err) => {

    console.log(err);
});




// Student Schema

const studentSchema = new mongoose.Schema({

    name: String,

    dept: String,

    sem: Number
});



// Student Model

const Student = mongoose.model(
    'students',
    studentSchema
);




// API Route

app.get('/students', async (req, res) => {

    try {

        const students = await Student.find();

        res.json(students);

    } catch (err) {

        res.status(500).json({
            message: err.message
        });
    }
});




// Start Server

app.listen(PORT, () => {

    console.log(
        `Server running at http://localhost:${PORT}`
    );
});
```

---

# Run Backend

```bash id="jlwmci"
node server.js
```

---

# FRONTEND SETUP

---

# Step 5: Create React Frontend

```bash id="jlwmcj"
npx create-react-app frontend
```

OR

```bash id="jlwmck"
npm create vite@latest frontend
```

---

# Step 6: React App

# frontend/src/App.js

```javascript id="jlwmcl"
import React, {
    useEffect,
    useState
} from 'react';

function App() {

    // Students State
    const [students, setStudents] = useState([]);

    // Search State
    const [search, setSearch] = useState("");



    // Fetch Students From Backend

    useEffect(() => {

        fetch('http://localhost:3000/students')

            .then((response) =>
                response.json()
            )

            .then((data) => {

                setStudents(data);
            });

    }, []);




    // Filter Logic

    const filteredStudents = students.filter(
        (student) => {

            return student.name
                .toLowerCase()
                .includes(
                    search.toLowerCase()
                );
        }
    );



    return (

        <div style={{ padding: "20px" }}>

            <h2>
                Student Search Filter
            </h2>



            <input
                type="text"

                placeholder="Search Student"

                value={search}

                onChange={(e) =>
                    setSearch(e.target.value)
                }

                style={{
                    padding: "8px",
                    width: "250px",
                    marginBottom: "20px"
                }}
            />



            <table
                border="1"
                cellPadding="10"

                style={{
                    borderCollapse: "collapse",
                    width: "600px"
                }}
            >

                <thead>

                    <tr>

                        <th>Name</th>
                        <th>Department</th>
                        <th>Semester</th>

                    </tr>

                </thead>



                <tbody>

                    {
                        filteredStudents.map(
                            (student) => (

                            <tr key={student._id}>

                                <td>
                                    {student.name}
                                </td>

                                <td>
                                    {student.dept}
                                </td>

                                <td>
                                    {student.sem}
                                </td>

                            </tr>
                        ))
                    }

                </tbody>

            </table>

        </div>
    );
}

export default App;
```

---

# frontend/src/index.js

```javascript id="jlwmcm"
import React from 'react';

import ReactDOM from 'react-dom/client';

import App from './App';

const root = ReactDOM.createRoot(
    document.getElementById('root')
);

root.render(
    <App />
);
```

---

# Run React Frontend

```bash id="jlwmcn"
npm start
```

OR

```bash id="jlwmco"
npm run dev
```

---

# Backend API URL

```text id="jlwmcp"
http://localhost:3000/students
```

---

# API Response

```json id="jlwmcq"
[
  {
    "_id": "6653c12345",
    "name": "Somu",
    "dept": "CSE",
    "sem": 5
  },
  {
    "_id": "6653c67890",
    "name": "Arun",
    "dept": "ECE",
    "sem": 4
  }
]
```

---

# Initial React Output

```text id="jlwmcr"
Name       Department   Semester

Somu       CSE          5
Arun       ECE          4
Priya      ISE          6
Karthik    CSE          3
Meena      EEE          7
```

---

# Search Example

## User Types

```text id="jlwmcs"
so
```

---

# Filtered Result

```text id="jlwmct"
Name       Department   Semester

Somu       CSE          5
```

---

# Concepts Used

## Backend

* Express.js
* MongoDB
* Mongoose
* REST API
* Async/Await
* JSON API

---

## Frontend

* React
* `useState`
* `useEffect`
* `fetch()`
* `filter()`
* `map()`

---

# Full MERN Architecture

```text id="jlwmcu"
React Frontend
       ↓ fetch()
Express Backend
       ↓
MongoDB Query
       ↓
Students Collection
       ↓
JSON Response
       ↓
React Filters & Displays
```
