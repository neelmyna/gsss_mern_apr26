# Question 9
```
Develop a React code to build a simple search filter functionality to display a filtered list based on the
search query entered by the user.
```

# Folder Structure

```text id="jlwmba"
project/
│
├── backend/
│   ├── server.js
│   └── package.json
│
└── frontend/
    ├── src/
    │   ├── App.js
    │   └── index.js
    └── package.json
```

---

# BACKEND — Express Server

---

# Step 1: Create Backend

```bash id="jlwmbb"
mkdir backend
cd backend
npm init -y
npm install express cors
```

---

# backend/server.js

```javascript id="jlwmbc"
const express = require('express');
const cors = require('cors');

const app = express();

const PORT = 3000;

app.use(cors());


// Student Array

const students = [

    {
        id: 1,
        name: "Somu",
        dept: "CSE",
        sem: 5
    },

    {
        id: 2,
        name: "Arun",
        dept: "ECE",
        sem: 4
    },

    {
        id: 3,
        name: "Priya",
        dept: "ISE",
        sem: 6
    },

    {
        id: 4,
        name: "Karthik",
        dept: "CSE",
        sem: 3
    },

    {
        id: 5,
        name: "Meena",
        dept: "EEE",
        sem: 7
    }
];


// API Route

app.get('/students', (req, res) => {

    res.json(students);
});


// Start Server

app.listen(PORT, () => {

    console.log(
        `Server running at http://localhost:${PORT}`
    );
});
```

---

# Run Backend

```bash id="jlwmbd"
node server.js
```

---

# FRONTEND — React Application

---

# Step 2: Create React Frontend

```bash id="jlwmbe"
npx create-react-app frontend
```

OR

```bash id="jlwmbf"
npm create vite@latest frontend
```

---

# frontend/src/App.js

```javascript id="jlwmbg"
import React, {
    useEffect,
    useState
} from 'react';

function App() {

    // Students State
    const [students, setStudents] = useState([]);

    // Search State
    const [search, setSearch] = useState("");



    // Fetch Students from Express Server

    useEffect(() => {

        fetch('http://localhost:3000/students')

            .then((response) =>
                response.json()
            )

            .then((data) => {

                setStudents(data);
            });

    }, []);




    // Filter Students

    const filteredStudents = students.filter(
        (student) => {

            return student.name
                .toLowerCase()
                .includes(
                    search.toLowerCase()
                );
        }
    );



    return (

        <div style={{ padding: "20px" }}>

            <h2>
                Student Search Filter
            </h2>


            <input
                type="text"
                placeholder="Enter Student Name"

                value={search}

                onChange={(e) =>
                    setSearch(e.target.value)
                }

                style={{
                    padding: "8px",
                    width: "250px",
                    marginBottom: "20px"
                }}
            />



            <table
                border="1"
                cellPadding="10"

                style={{
                    borderCollapse: "collapse",
                    width: "600px"
                }}
            >

                <thead>

                    <tr>
                        <th>ID</th>
                        <th>Name</th>
                        <th>Department</th>
                        <th>Semester</th>
                    </tr>

                </thead>


                <tbody>

                    {
                        filteredStudents.map(
                            (student) => (

                            <tr key={student.id}>

                                <td>
                                    {student.id}
                                </td>

                                <td>
                                    {student.name}
                                </td>

                                <td>
                                    {student.dept}
                                </td>

                                <td>
                                    {student.sem}
                                </td>

                            </tr>
                        ))
                    }

                </tbody>

            </table>

        </div>
    );
}

export default App;
```

---

# frontend/src/index.js

```javascript id="jlwmbh"
import React from 'react';
import ReactDOM from 'react-dom/client';

import App from './App';

const root = ReactDOM.createRoot(
    document.getElementById('root')
);

root.render(
    <App />
);
```

---

# Run React Frontend

```bash id="jlwmbi"
npm start
```

OR (Vite)

```bash id="jlwmbj"
npm run dev
```

---

# Backend API Output

URL:

```text id="jlwmbk"
http://localhost:3000/students
```

Response:

```json id="jlwmbl"
[
  {
    "id": 1,
    "name": "Somu",
    "dept": "CSE",
    "sem": 5
  },
  {
    "id": 2,
    "name": "Arun",
    "dept": "ECE",
    "sem": 4
  }
]
```

---

# React UI Initial Output

```text id="jlwmbm"
ID   Name      Department   Semester

1    Somu      CSE          5
2    Arun      ECE          4
3    Priya     ISE          6
4    Karthik   CSE          3
5    Meena     EEE          7
```

---

# Search Example

## User Types

```text id="jlwmbn"
ar
```

---

# Filtered Result

```text id="jlwmbo"
ID   Name      Department   Semester

2    Arun      ECE          4
```

---

# Concepts Used

## Backend

* Express.js
* REST API
* `app.get()`
* JSON Response
* CORS

---

## Frontend

* React
* `useState`
* `useEffect`
* `fetch()`
* `filter()`
* `map()`

---

# Architecture Flow

```text id="jlwmbp"
React Frontend
       ↓ fetch()
Express API Server
       ↓
Student Array
       ↓
JSON Response
       ↓
React Filters & Displays
```
